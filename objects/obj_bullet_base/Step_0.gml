/// @description event

// Inherit the parent event
event_inherited();

check_out_of_bounds(); // Check if the bullet is out of bounds
reduce_lifetime();     // Reduce lifetime and destroy if expired

// Call the function to check if the bullet missed
//check_bullet_miss(shooter);
